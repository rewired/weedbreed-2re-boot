{
  "name": "@wb/tools-monitor",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "node -e \"console.log('Skipping tools-monitor build (pending type refactor)')\"",
    "test": "vitest run",
    "lint": "eslint --max-warnings=0 \"src/**/*.ts\" \"tests/**/*.ts\"",
    "format": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\"",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "mock-server": "tsx scripts/mockTelemetryServer.mts"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@wb/transport-sio": "workspace:*",
    "neo-blessed": "^0.2.0",
    "socket.io-client": "^4.8.1",
    "zod": "3.24.0"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.25"
  }
}
